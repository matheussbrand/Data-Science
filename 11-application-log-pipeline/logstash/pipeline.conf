input { kafka { bootstrap_servers => "kafka:9092" topics => ["application-logs"] codec => json } }
filter { mutate { add_field => { "pipeline" => "portfolio" } } }
output { stdout { codec => rubydebug } }
