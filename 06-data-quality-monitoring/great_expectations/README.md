# Great Expectations

Configure uma suíte com expectativas equivalentes às regras em `src/validate.py`.

Exemplos:
- `expect_column_values_to_not_be_null`
- `expect_column_values_to_be_unique`
- `expect_column_values_to_be_between`

A ideia é separar **dados inválidos** de **pipeline quebrado**.
